.form-group
	//- p There are {{posts.length}} posts
	div(ng-repeat='post in posts')
		a(href="readPost/{{post.id}}" ,style="text-align: center; cursor: pointer;")
			h3
				b {{post.title}}
		div(style="text-align: center;")
			img.img-thumbnail(src="uploads/{{post.image}}", style="height: 200px")
		div
			p(style="color: black", ng-bind-html='post.text')
